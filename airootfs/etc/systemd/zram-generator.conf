# Con 64 GB de RAM, zram es un colchón, no un sustituto de la RAM.
# 8 GB comprimidos bastan y evitan tocar el NVMe en picos de compilación.
[zram0]
zram-size = 8192
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
